{
  "_comment": [
    "Reference copy of every setting at its shipped value. This file is NOT read at runtime —",
    "the defaults live in config/Config.qml. Copy the keys you want into",
    "~/.config/quickshell/ionix/config.json; that file is deep-merged over these and applied",
    "live, so saving it restyles the running shell with no restart.",
    "Arrays replace wholesale rather than appending.",
    "",
    "Five layers are merged, lowest priority first: these defaults, then theme.json (written by",
    "ionixtheme), then the active theme's option overlays (below), then settings.json (written",
    "by the start menu's settings page — the only file the shell itself writes), then config.json",
    "(yours). config.json therefore beats the settings page: a key you set by hand there makes the",
    "matching switch inert, and the page shows those rows as 'Pinned by config.json' rather than",
    "letting them flip and do nothing.",
    "",
    "Theme options: a theme.json may declare an 'options' array, each entry",
    "  { key, title, subtitle, glyph, default: bool, on: {config patch}, off: {config patch} },",
    "which the settings page renders as a switch under 'Theme options'. Flipping one merges that",
    "entry's 'on' or 'off' patch just above the theme's own values — so both files above still win",
    "— and stores the value in settings.json under themes.<themename>.<key>, keyed by theme so",
    "switching away and back keeps it. Both patches are optional: leaving 'off' out means the",
    "theme's plain values already are the off state. Only booleans are rendered: a setting that is",
    "genuinely one-of-N belongs in the settings page's own rows, where it applies under every theme",
    "rather than only the one that thought to declare it — bar.position is the worked example.",
    "",
    "Do not put 'options' here: options are declared by a theme, and these defaults are not one."
  ],

  "bar": {
    "_comment": "position is top | bottom | left | right, and is offered as a picker in the start menu's settings page for every theme — so a theme naming one here is stating a default, not a fact, and anything you pick there outlives a theme switch. The last two dock the bar to a screen edge as a vertical strip and every module stacks and re-anchors itself — popouts, menus and tooltips open sideways, the clock switches to clock.verticalFormat, and the taskbar and workspaces run top-to-bottom. `height` is the bar's THICKNESS whichever edge it is on, so it is the width of a vertical bar; Theme.pillHeight is derived from it and every module sizes its short axis to that. In `margin`, `top` is the gap at each end of the bar's long axis and `left` the gap from the edge it is docked to — a vertical bar swaps which is which, so the same two numbers keep meaning the same two gaps. All of it is ignored while floating is false.",
    "height": 46,
    "position": "top",
    "floating": true,
    "radius": 16,
    "opacity": 0.82,
    "nativeBlur": true,
    "margin": { "top": 6, "left": 8, "right": 8 },
    "monitors": ["*"]
  },

  "modules": {
    "left": ["Launcher", "Workspaces", "Taskbar"],
    "center": ["MediaWidget"],
    "right": [
      "Tray",
      "AudioIndicator",
      "NetworkIndicator",
      "BluetoothIndicator",
      "HueIndicator",
      "BatteryIndicator",
      "Clock",
      "NotificationBell"
    ]
  },

  "theme": {
    "_comment": "Any key here overrides config/Theme.qml. Shown with the built-in values.",
    "bgDeep": "#0f0a18",
    "bgWindow": "#1a1025",
    "bgCard": "#2d2d3f",
    "hover": "#3a2d50",
    "border": "#4a3d60",
    "muted": "#6b5f80",
    "text": "#c4b5fd",
    "textBright": "#e2d9f3",
    "accent": "#9b59b6",
    "accentBright": "#a855f7",
    "accentLight": "#c084fc",
    "green": "#2ecc71",
    "orange": "#f39c12",
    "red": "#e74c3c",
    "cyan": "#48dbfb",
    "link": "#7c3aed",
    "fontFamily": "JetBrainsMono Nerd Font",
    "fontMono": "JetBrainsMono Nerd Font",
    "fontLogo": "Ionix"
  },

  "launcher": {
    "icon": "",
    "command": ["ioexplorer-spotlight"],
    "middleCommand": ["ioexplorer-spotlight"]
  },

  "start": {
    "_comment": "The start menu behind the logo button. enabled:false gives the button back to launcher.command. maxHeightFraction is of the screen height — past that the menu body scrolls instead of growing. defaultPins is only a seed: as soon as you pin or unpin anything the list moves to $XDG_STATE_HOME/ionix/quickshell/start.json, which also holds the launch counts behind the Frequent section. Pins are desktop-entry ids without the '.desktop' suffix; ones naming apps you don't have are skipped, not drawn empty. The footer is titled with displayName, or when that is empty the account's full name (chfn -f), or the login; whenever the title is not the login, the login shows under it as user@host. avatar is a picture path (~ allowed); empty means the first of $XDG_CONFIG_HOME/quickshell/avatar.png and ~/.face that exists, re-checked every time the menu opens.",
    "enabled": true,
    "width": 620,
    "maxHeightFraction": 0.72,
    "columns": 6,
    "iconSize": 40,
    "showRunning": true,
    "recommend": "frequent",
    "recommendCount": 8,
    "displayName": "",
    "avatar": "",
    "defaultPins": ["io.github.ionix.IoExplorer", "kitty", "firefox", "code", "tabby", "steam"],
    "stylerDir": "/usr/local/share/ionix/styler",
    "stylerBin": "/usr/local/share/ionix/styler/bin/ionixtheme",
    "stateFile": ""
  },

  "workspaces": {
    "_comment": "Workspaces are filtered to the monitor the bar is on. persistent pads the list with empty slots and is 0 by default because Hyprland assigns workspace IDs dynamically across monitors; raise it only with per-monitor workspace rules.",
    "persistent": 0,
    "showEmpty": true
  },

  "taskbar": {
    "enabled": true,
    "maxWidth": 420,
    "iconSize": 22,
    "currentWorkspaceOnly": false,
    "iconOverrides": {}
  },

  "media": {
    "backend": "auto",
    "maxWidth": 180,
    "preferred": "toxen"
  },

  "clock": {
    "_comment": "The vertical* formats are used instead when bar.position is left or right: 'HH:mm' at the bar font is wider than a 46px vertical bar, so the parts stack. Qt.formatDateTime passes anything it does not recognise through as a literal, which is how the embedded newline survives. timer is the countdown on the clock's right click: sound is played with pw-play and repeated beepGap ms after it ends until the pill is clicked; presets are lengths in seconds shown under the digits.",
    "format": "HH:mm",
    "dateFormat": "ddd d MMM",
    "showDate": true,
    "verticalFormat": "HH\nmm",
    "verticalDateFormat": "dd/MM",
    "timer": {
      "sound": "/usr/share/sounds/freedesktop/stereo/alarm-clock-elapsed.oga",
      "beepGap": 400,
      "presets": [60, 300, 600, 1500]
    }
  },

  "audio": {
    "step": 0.02,
    "maxVolume": 1.0
  },

  "brightness": {
    "step": 0.05,
    "device": ""
  },

  "network": { "enabled": true },
  "bluetooth": { "enabled": true },

  "hue": {
    "_comment": "Philips Hue bar module, off by default. Set enabled:true and the bar shows a bulb; click it to find and pair a bridge. The bridge address and the credential it issues are NOT set here — they are written to $XDG_STATE_HOME/ionix/quickshell/hue.json along with your pinned lights, because this config is read-only. Lights are only polled while the popout is open, so pollInterval is a refresh rate you can see, not a background cost. cloudDiscovery:false skips discovery.meethue.com and leaves only manual IP entry.",
    "enabled": false,
    "cloudDiscovery": true,
    "pollInterval": 2000,
    "transitionTime": 300,
    "presets": ["#ff4d4d", "#ff9f43", "#ffd166", "#2ecc71", "#48dbfb", "#7c3aed", "#c084fc", "#ff7bd5"],
    "stateFile": ""
  },

  "battery": {
    "warnAt": 30,
    "criticalAt": 15
  },

  "calendar": {
    "_comment": "provider is none | local | outlook, and is offered as a picker in the settings page. local is the built-in read/write calendar (a JSON file, local.file, with simple daily/weekly/monthly/yearly repeats — add and edit events from the popout, or `ionix-calendar add`); a remote provider adds its account's events on top of the local ones rather than replacing them. Anything but none starts the ionix-calendar daemon as a child of the shell; it syncs every syncInterval seconds (and when the popout opens) into $XDG_STATE_HOME/ionix/quickshell/calendar/, which the popout reads — dots on days, a per-day agenda, a Join button on events with a link. showNext puts the next meeting in the clock pill when it starts within upcomingWindow minutes; reminders sends a notification reminderMinutes before each meeting. calendars is an include-list of calendar names or ids (`ionix-calendar calendars` prints them); empty means all. For outlook, clientId is your Entra app registration's Application (client) ID — a public client with device-code flow allowed, not a secret — and tenant is a tenant id/domain or organizations | consumers | common. Tokens go to the keyring via secret-tool, or a 0600 file under stateDir when there is no Secret Service.",
    "provider": "local",
    "local": {
      "file": "",
      "name": "Local",
      "color": ""
    },
    "bin": "ionix-calendar",
    "syncInterval": 300,
    "pastDays": 31,
    "futureDays": 92,
    "calendars": [],
    "hideDeclined": true,
    "showNext": true,
    "upcomingWindow": 60,
    "nextMaxWidth": 140,
    "reminders": true,
    "reminderMinutes": 5,
    "stateDir": "",
    "outlook": {
      "clientId": "",
      "tenant": "organizations"
    }
  },

  "notifications": {
    "popups": true,
    "timeout": 5000,
    "maxPopups": 3,
    "width": 380,
    "monitor": ""
  },

  "osd": {
    "enabled": true,
    "timeout": 1600,
    "margin": 120
  },

  "windowSwitcher": {
    "_comment": "The Alt+Tab overlay. currentWorkspaceOnly:false offers every window everywhere in most-recently-used order, which is what the pre-overlay Alt+Tab bind reached; true narrows it to the focused workspace, Windows-style. livePreviews:false swaps the screencopy thumbnails for icon tiles — each visible card is otherwise a continuous capture stream. maxWidthFraction is of the screen width; past that the card grid wraps to further rows.",
    "enabled": true,
    "currentWorkspaceOnly": false,
    "livePreviews": true,
    "cardWidth": 240,
    "cardHeight": 170,
    "maxWidthFraction": 0.8
  },

  "power": {
    "lock": ["hyprlock", "-c", "/etc/hypr/hyprlock.conf", "--grace", "2"],
    "logout": ["uwsm", "stop"],
    "suspend": ["systemctl", "suspend"],
    "hibernate": ["systemctl", "hibernate"],
    "reboot": ["systemctl", "reboot"],
    "shutdown": ["systemctl", "poweroff"]
  }
}
